import math
import json
import array

a = array.array('i',[23,24,25,26,27,28,29])
print(a)

# Gives the data type by which the array is initialized
print(a.typecode)

# Gives the Byte of each element in an array
print(a.itemsize)

print(a.tolist())